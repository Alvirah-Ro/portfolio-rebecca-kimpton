export default function ConversionExplanation() {
    return (
            <div className="row mx-5 mt-4 mb-0 p-3">
                At work, I frequently need to convert foreign currencies into U.S. dollars while reviewing invoices. Often, the invoices list the seller’s price and discount but leave out the actual dealer cost. This means I have to calculate the dealer cost first and then convert it separately into dollars. I created this page to streamline the process by handling both calculations at once.
            </div>
    );
}
